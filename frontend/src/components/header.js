import React from "react";

const Header = () => {
    return (
            <header className="header-container header">
                <h1>chatbot</h1>
                <img className="logo" src="./logo-llama.svg" alt="logo chatbot"/>
            </header>
    );
};

export default Header;